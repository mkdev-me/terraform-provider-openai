---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openai_project_user Resource - terraform-provider-openai"
subcategory: ""
description: |-
  Manages a user's membership and roles in an OpenAI Project. This resource adds a user to a project and assigns them one or more project-level roles.
---

# openai_project_user (Resource)

Manages a user's membership and roles in an OpenAI Project. This resource adds a user to a project and assigns them one or more project-level roles.

## Example Usage

```terraform
# First, create a project
resource "openai_project" "production" {
  name = "Production API"
}

# Look up project-level roles
data "openai_project_role" "member" {
  project_id = openai_project.production.id
  name       = "member"
}

# Add a user to the project with one or more roles
resource "openai_project_user" "developer" {
  project_id = openai_project.production.id
  user_id    = var.user_id
  role_ids   = [data.openai_project_role.member.id]
}

output "user_email" {
  value = openai_project_user.developer.email
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `project_id` (String) The ID of the project.
- `role_ids` (Set of String) Set of project-level role IDs to assign to the user. Must be project roles (e.g. from the `openai_project_role` data source). At least one role is required.
- `user_id` (String) The ID of the user.

### Read-Only

- `added_at` (Number) The Unix timestamp (in seconds) when the user was added to the project.
- `email` (String) The email of the user.
- `id` (String) The identifier of the project user (project_id:user_id).
